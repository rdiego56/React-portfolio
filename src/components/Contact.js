import React from 'react';

const Contact = ({
  name,
  email,
  message,
  isEmailValid,
  onInputChange,
  onEmailBlur,
  onFormSubmit,
}) => {
  return (
    <section>
      <h2>Contact</h2>
      {/* Contact form with inputs */}
    </section>
  );
};

export default Contact;