import React from "react";

const Header = ({ currentSection, onNavigationClick}) => {
    return (
        <header>
            <h1> Deigos Portfolio</h1>
            <nav>

            </nav>
        </header>
    );
    
};

export default Header;
